---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-data:
      line-length:
        max: 120
platforms:
  - name: instance
    image: leandelivery/docker-systemd:ubuntu-18.04
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: tomcat7
verifier:
  name: testinfra
  options:
    sudo: true
  lint:
    name: flake8
